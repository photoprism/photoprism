# Uncomment to enable debug mode:
# log:
#  level: DEBUG

serversTransport:
  # Required to proxy services with self-signed HTTPS certificates:
  insecureSkipVerify: true

# Open ports and protocols (HTTP will be redirected to HTTPS):
entryPoints:
  web:
    address: ":80"
    http:
      # Drop request headers whose name holds a character other than a letter, digit or dash, as
      # CGI/WSGI/PHP/NGINX backends read "X_Auth_Token" and "X-Auth-Token" as one variable:
      aliasHeadersStrategy: delete
      # Refuse an encoded null byte, since no supported filesystem admits one in a file name. The
      # other encoded characters stay at the Traefik default of true, because a file name may
      # legitimately contain them and the download and WebDAV routes address files by name.
      encodedCharacters:
        allowEncodedNullCharacter: false
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
    http:
      aliasHeadersStrategy: delete
      encodedCharacters:
        allowEncodedNullCharacter: false
    transport:
      respondingTimeouts:
        readTimeout: "3h"
        writeTimeout: "0s"

certificatesResolvers:
  myresolver:
    # See https://doc.traefik.io/traefik/https/acme/
    acme:
      email: tls@yourdomain.com
      storage: /data/letsencrypt.json
      httpChallenge:
        entryPoint: web

providers:
  # Always keep this:
  docker:
    exposedByDefault: false
    watch: true

# Disable API & Dashboard by default, please read Traefik docs before enabling this:
api:
  insecure: false
  dashboard: false